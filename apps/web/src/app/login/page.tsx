export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center p-6">
      <div className="max-w-sm w-full border border-border rounded-md p-6">
        <h1 className="text-lg font-semibold mb-2">Sign in</h1>
        <p className="text-sm text-muted-foreground">Auth is not yet wired in dev mode. The CRM runs single-org with DEFAULT_ORG_ID.</p>
      </div>
    </div>
  );
}
